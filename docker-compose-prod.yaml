version: '3.3'

services:
  codecamp__freeboard:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - 3000:3000
    # volumes:
    #   - ./src:/codecamp__freeboard/src
